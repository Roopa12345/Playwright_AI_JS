//let class ="Hello World";
//class  is a reserved keyword in javascript, so it can't be used as a identifier

//let if = true
//if  is a reserved keyword in javascript, so it can't be used as a identifier

//let return = 5;
//return is a reserved keyword in javascript, so it can't be used as a identifier

let myVariable = 10;
//This is a valid identifier and can be used to store a value




